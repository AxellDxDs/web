import { type ClassValue, clsx } from 'clsx';\nimport { twMerge } from 'tailwind-merge';\nimport { formatDistance, differenceInDays, differenceInHours, differenceInMinutes, differenceInSeconds } from 'date-fns';\nimport { id as idLocale } from 'date-fns/locale';\n\nexport function cn(...inputs: ClassValue[]) {\n  return twMerge(clsx(inputs));\n}\n\nexport function calculateDaysBetween(startDate: Date, endDate: Date): number {\n  return Math.floor((endDate.getTime() - startDate.getTime()) / (1000 * 60 * 60 * 24));\n}\n\nexport function formatCountdown(targetDate: Date): {\n  days: number;\n  hours: number;\n  minutes: number;\n  seconds: number;\n  total: string;\n} {\n  const now = new Date();\n  const diff = targetDate.getTime() - now.getTime();\n\n  if (diff <= 0) {\n    return { days: 0, hours: 0, minutes: 0, seconds: 0, total: 'Selesai' };\n  }\n\n  const days = Math.floor(diff / (1000 * 60 * 60 * 24));\n  const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));\n  const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));\n  const seconds = Math.floor((diff % (1000 * 60)) / 1000);\n\n  return {\n    days,\n    hours,\n    minutes,\n    seconds,\n    total: `${days}h ${hours}j ${minutes}m ${seconds}d`,\n  };\n}\n\nexport function getZodiacSign(birthDate: Date): string {\n  const month = birthDate.getMonth() + 1;\n  const date = birthDate.getDate();\n\n  const zodiacSigns = [\n    { name: 'Capricorn', start: [12, 22], end: [1, 19] },\n    { name: 'Aquarius', start: [1, 20], end: [2, 18] },\n    { name: 'Pisces', start: [2, 19], end: [3, 20] },\n    { name: 'Aries', start: [3, 21], end: [4, 19] },\n    { name: 'Taurus', start: [4, 20], end: [5, 20] },\n    { name: 'Gemini', start: [5, 21], end: [6, 20] },\n    { name: 'Cancer', start: [6, 21], end: [7, 22] },\n    { name: 'Leo', start: [7, 23], end: [8, 22] },\n    { name: 'Virgo', start: [8, 23], end: [9, 22] },\n    { name: 'Libra', start: [9, 23], end: [10, 22] },\n    { name: 'Scorpio', start: [10, 23], end: [11, 21] },\n    { name: 'Sagittarius', start: [11, 22], end: [12, 21] },\n  ];\n\n  for (const sign of zodiacSigns) {\n    const [startMonth, startDate] = sign.start;\n    const [endMonth, endDate] = sign.end;\n\n    if (startMonth === endMonth) {\n      if (month === startMonth && date >= startDate && date <= endDate) {\n        return sign.name;\n      }\n    } else if (startMonth < endMonth) {\n      if ((month === startMonth && date >= startDate) || (month === endMonth && date <= endDate)) {\n        return sign.name;\n      }\n    } else {\n      if ((month === startMonth && date >= startDate) || (month === endMonth && date <= endDate)) {\n        return sign.name;\n      }\n    }\n  }\n\n  return 'Unknown';\n}\n\nexport function getNextBirthday(birthDate: Date): { date: Date; daysUntil: number } {\n  const today = new Date();\n  let nextBirthday = new Date(today.getFullYear(), birthDate.getMonth(), birthDate.getDate());\n\n  if (nextBirthday < today) {\n    nextBirthday = new Date(today.getFullYear() + 1, birthDate.getMonth(), birthDate.getDate());\n  }\n\n  const daysUntil = calculateDaysBetween(today, nextBirthday);\n\n  return { date: nextBirthday, daysUntil };\n}\n\nexport function getAge(birthDate: Date): number {\n  const today = new Date();\n  let age = today.getFullYear() - birthDate.getFullYear();\n  const monthDiff = today.getMonth() - birthDate.getMonth();\n\n  if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < birthDate.getDate())) {\n    age--;\n  }\n\n  return age;\n}\n\nexport function shuffleArray<T>(array: T[]): T[] {\n  const shuffled = [...array];\n  for (let i = shuffled.length - 1; i > 0; i--) {\n    const j = Math.floor(Math.random() * (i + 1));\n    [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];\n  }\n  return shuffled;\n}\n\nexport function getRandomItem<T>(array: T[]): T {\n  return array[Math.floor(Math.random() * array.length)];\n}\n\nexport function clamp(value: number, min: number, max: number): number {\n  return Math.max(min, Math.min(max, value));\n}\n\nexport function interpolateColor(startColor: string, endColor: string, factor: number): string {\n  const start = parseInt(startColor.slice(1), 16);\n  const end = parseInt(endColor.slice(1), 16);\n\n  const r = Math.round(((start >> 16) & 255) + (((end >> 16) & 255) - ((start >> 16) & 255)) * factor);\n  const g = Math.round(((start >> 8) & 255) + (((end >> 8) & 255) - ((start >> 8) & 255)) * factor);\n  const b = Math.round((start & 255) + ((end & 255) - (start & 255)) * factor);\n\n  return `#${((r << 16) | (g << 8) | b).toString(16).padStart(6, '0')}`;\n}\n\nexport function formatTime(seconds: number): string {\n  const hrs = Math.floor(seconds / 3600);\n  const mins = Math.floor((seconds % 3600) / 60);\n  const secs = Math.floor(seconds % 60);\n\n  if (hrs > 0) {\n    return `${hrs}:${mins.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`;\n  }\n  return `${mins}:${secs.toString().padStart(2, '0')}`;\n}\n\nexport function getPasswordHint(attempts: number): string {\n  if (attempts < 2) return \"Kode salah, coba lagi sayang\";\n  if (attempts === 2) return \"Masih salah, ingat-ingat tanggal spesial kita\";\n  if (attempts === 3) return \"Coba ingat tanggal spesial kita — 24 Maret 2026\";\n  if (attempts === 4) return \"Hint: Format DDMMYY dari tanggal jadian kita\";\n  return \"Coba lagi, jangan menyerah\";\n}\n\nexport function isValidPassword(password: string): boolean {\n  return password === '24526' || password === '24/5/26' || password === '24-05-26';\n}\n\nexport function smoothstep(t: number): number {\n  return t * t * (3 - 2 * t);\n}\n\nexport function easeInOutQuad(t: number): number {\n  return t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;\n}\n\nexport function easeOutElastic(t: number): number {\n  const c5 = (2 * Math.PI) / 4.5;\n  return t === 0\n    ? 0\n    : t === 1\n    ? 1\n    : Math.pow(2, -10 * t) * Math.sin((t * 10 - 0.75) * c5) + 1;\n}\n\nexport function getCompatibilityMessage(percentage: number): string {\n  if (percentage >= 90) return \"Bintang-bintang sangat sepakat kalian adalah pasangan sempurna\";\n  if (percentage >= 80) return \"Kompatibilitas tinggi, cinta kalian adalah takdir\";\n  if (percentage >= 70) return \"Hubungan yang kuat dan bermakna menanti kalian\";\n  if (percentage >= 60) return \"Banyak hal yang bisa dijalani bersama\";\n  return \"Perlu usaha, tapi cinta bisa menaklukan semua\";\n}\n