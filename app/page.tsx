'use client';\n\nimport { useEffect, useState } from 'react';\nimport useAppStore from '@/lib/store';\nimport { cn } from '@/lib/utils';\n\nexport default function Home() {\n  const { currentSection, goToSection, unlockedSections } = useAppStore();\n  const [mounted, setMounted] = useState(false);\n\n  useEffect(() => {\n    setMounted(true);\n  }, []);\n\n  if (!mounted) {\n    return (\n      <div className=\"w-full h-screen flex items-center justify-center\">\n        <div className=\"text-center\">\n          <div className=\"animate-pulse mb-4\">\n            <span className=\"text-4xl\">💕</span>\n          </div>\n          <p className=\"text-text-secondary\">Memuat cerita cinta kita...</p>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <main className=\"w-full min-h-screen\">\n      {/* Navigation Dots */}\n      <nav className=\"fixed bottom-8 left-1/2 transform -translate-x-1/2 z-50\">\n        <div className=\"flex gap-3 backdrop-blur-md bg-black/20 px-6 py-4 rounded-full border border-white/10\">\n          {Array.from({ length: 22 }, (_, i) => i + 1).map((num) => (\n            <button\n              key={num}\n              onClick={() => goToSection(num)}\n              disabled={!unlockedSections.includes(num)}\n              className={cn(\n                'w-2 h-2 rounded-full transition-all duration-300',\n                currentSection === num\n                  ? 'bg-accent-pink scale-150'\n                  : unlockedSections.includes(num)\n                  ? 'bg-text-secondary hover:bg-accent-pink cursor-pointer'\n                  : 'bg-text-muted cursor-not-allowed opacity-50'\n              )}\n              title={`Section ${num}`}\n            />\n          ))}\n        </div>\n      </nav>\n\n      {/* Content Area - Placeholder untuk sections */}\n      <div className=\"w-full h-screen flex items-center justify-center\">\n        <div className=\"text-center space-y-6\">\n          <div className=\"text-6xl animate-bounce\">💕</div>\n          <h1 className=\"text-4xl font-bold bg-gradient-to-r from-accent-pink to-accent-lavender bg-clip-text text-transparent\">\n            Naura & Farsya\n          </h1>\n          <p className=\"text-xl text-text-secondary max-w-lg mx-auto\">\n            Cerita cinta kita dimulai dari sini. Tekan navigasi di bawah untuk menjelajahi.\n          </p>\n          <div className=\"pt-8\">\n            <button\n              onClick={() => goToSection(2)}\n              className=\"px-8 py-3 rounded-full bg-gradient-btn text-white font-semibold hover:shadow-lg hover:scale-105 transition-all\"\n            >\n              Mulai Cerita →\n            </button>\n          </div>\n        </div>\n      </div>\n    </main>\n  );\n}\n